# Samples

Each sample includes a Nexus YAML definition file and generated output for all supported languages.

- **[user-service](user-service)** — Basic service with CRUD operations and `$ref` types.
- **[inventory-service-with-predefined-models](inventory-service-with-predefined-models)** — Demonstrates `$langRef` for referencing existing language-specific types.
